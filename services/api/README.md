# services/api

This directory is reserved for future lightweight service endpoints.

Initial rule:
- keep the boundary narrow
- add service logic only when the web console requires it
- avoid premature backend complexity
